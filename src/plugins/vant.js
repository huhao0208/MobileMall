import Vue from "vue"
import {Button,Tabbar, TabbarItem,NavBar,Swipe,SwipeItem, Lazyload,Toast,Notify ,Row, Col,Image,Grid, GridItem,
    Card,Loading,PullRefresh,List,Cell ,CellGroup,Tab, Tabs,ImagePreview  ,Divider ,Stepper ,SwipeCell ,Dialog ,SubmitBar 

} from 'vant'

Vue.use(Button)
.use(Tabbar).use(TabbarItem)
.use(Swipe)
.use(SwipeItem)
.use( Lazyload)
.use(Toast )
.use(Notify )
.use(Row)
.use(Col)
.use(Image )
.use(Grid)
.use(GridItem)
.use(Card)
.use(Loading)
.use(NavBar )
.use(PullRefresh )
.use( List )
.use( Cell)
.use( CellGroup )
.use( Tab)
.use( Tabs)
.use( ImagePreview )
.use( Divider )
.use( Stepper )
.use( SwipeCell)
.use( Dialog)
.use( SubmitBar)